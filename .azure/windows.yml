jobs:
- job: floskell_test_windows
  timeoutInMinutes: 120
  pool:
    vmImage: 'windows-2019'
  steps:
  - bash: |
      ls $(cygpath $ProgramData)/chocolatey/lib
      choco install -y -dv --ignore-dependencies ghc --version=8.8.4 -m
      choco install -y --ignore-dependencies ghc --version=8.10.1 -m
      choco install -y --ignore-dependencies ghc --version=8.10.2 -m
      choco install -y cabal
      $(cygpath $ProgramData)/chocolatey/bin/RefreshEnv.cmd
      export GHCS_PATH=$(cygpath $ProgramData)/chocolatey/lib/ghc/tools
      export PATH=$GHCS_PATH/ghc-8.10.2/bin:$GHCS_PATH/ghc-8.10.1/bin:$GHCS_PATH/ghc-8.8.4/bin:$PATH
      cabal update
      cabal build -w ghc-8.8.4
      cabal build -w ghc-8.10.1
      cabal build -w ghc-8.10.2
    displayName: 'cabal build'
